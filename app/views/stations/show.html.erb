<h1><%= @station.name %></h1>

<ul>
  <li>
    <%= link_to "Edit", edit_station_path(@station) %>
  </li>
  <li>
    <%= link_to "Delete", station_path(@station), data: {
      turbo_method: :delete,
      turbo_confirm: "Are you sure?"
    } %>
  </li>
</ul>
